# 58 — Casting commitment: integration build (placeholder until 57 specs it)

**Type:** build (SKSE C++ and/or Nemesis behavior — NO Papyrus ships)

**Status:** blocked. **Blocked by:** 57. Part of the ticket 53 umbrella.

## What this is

The build ticket for whatever ticket 57's owner-ruled, second-model-reviewed spec says:
adopt Enemy Magelock (+ compatibility patch), adapt its state machinery under our own mod
code, port CARIM's edge-driven AV mechanism into SH2's DLL, or a per-cast-type composition.
Do not start from this file — 57's spec replaces this section when it lands.

## Standing rules that bind the build regardless of verdict

- One-state prototype first (right-hand aimed concentration), validated headlessly against
  ticket 54's calibration signatures BEFORE the single owner feel test; only then the matrix.
- No mechanism extrapolation without a one-state live probe (ticket 33's lesson, three times).
- Kill criteria from 57's spec are stop-lines, not suggestions.
- FOMOD work only after the mechanism is owner-certified.
- Cleanup rides along: remove `houseCARL - SpellHotbar_RootedConcentration` (rejected trial),
  revert the inert DLL movement capture (`98350c9`'s input.cpp/combo_cache.h half) in the first
  DLL change, land ADR-0015's fourth amendment, correct playbook pointers.

## Acceptance

Ticket 33's inherited list, plus ticket 53's additions (owner feel sign-offs for NPC channel
and player moving entry; deliberate contention table; calibration signatures cited by path).
