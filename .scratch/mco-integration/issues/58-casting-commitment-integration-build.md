# 58 — Casting commitment: structural root in our own behavior patch

**Type:** build (Nemesis behavior — NO Papyrus, NO SPID, no ESP records)

**Status:** BUILT and live-verified 2026-08-29 (patch `shcr`, commit `3af9755`). Player and NPC
rooting owner-confirmed live; see the build/verification record at the end of this file. Open:
rotation-tracking owner confirmation, FOMOD option.
**Blocked by:** None. 54 closed partial, 55/56/57 closed — read
`notes/57-mechanism-comparison.md` before touching anything here.

## The mechanism, decided

Commitment comes from the state the cast runs in, not from a flag planted on an existing one.
Every flag-based attempt failed (ticket 33, three times), and neither reference implementation
uses one: Enemy Magelock's only `BSIsActiveModifier` binds `bAllowRotation`, and measurement on
ticket 54 showed `bAnimationDriven` never rises on the MSCO fire-and-forget cast the owner
certifies as the correct root.

**What Magelock does, and what we borrow:** it replaces the *generators* behind the vanilla
nodes where casting meets locomotion — `MagicCastingLocomotionState` (`#0926`),
`MagicCast_Standing` (`#0930`), `MagicCast_TurnLeft_State`, `MagicCast_TurnRight_State` — so
casting stops routing through a locomotion-blending state and there is nothing left to
translate the actor. Rotation is handled separately by a `BSIsActiveModifier` binding
`bAllowRotation`. Its clips are vanilla; the DAR `_CustomConditions\94010` folder is a
file-override carrier, not new animation.

**Where ours differs: it applies globally.** Magelock carries a two-state machine
(`#altmag$174 BNPCBehavior` → `NPCMagicBehavior` / `PlayerMagicBehavior`) whose `startStateId`
binds to a graph variable, purely to treat the player differently. We do not want that split —
the standing rule is everything roots, every actor, movement out of scope — so that machinery,
and the open question about which variable index 4 names, is dropped rather than reproduced.
Likewise no per-cast-type discrimination: SBF's `iSBF_LeftCastType` / `iSBF_RightCastType` are
available and already installed (mod code `sbeef`), but nothing in the requirement needs them.
Noted here so a later requirement can pick them up without rediscovering them.

## Build steps

1. **Contention read first.** The nodes above sit in territory `msco`, `pscd`, `sbeef`, and
   `shtb` already patch, and Nemesis resolves single-value conflicts last-checked-wins — this
   bit us on ticket 33, where `shcc` silently displaced `sbeef` and `pscd` edits. Produce the
   table of every node we intend to touch against every mod code that touches it, and decide
   each displacement deliberately before authoring. `sbeef` is both a thing not to break and a
   framework already relied on elsewhere.
2. **Retire `shcc`.** It is the failed flag-plant patch (`nemesis/Nemesis_Engine/mod/shcc/`,
   generator `.scratch/shcc-build/build_shcc.py`), deployed inert. The new patch replaces it;
   do not layer on top of it.
3. **Author the patch** under its own mod code, generated by a script in the repo the way
   `shcc` was — the file set is regenerable, not hand-maintained.
4. **One state first.** Prove a single cast path roots before building the matrix (ticket 33's
   lesson, learned three times). Nemesis needs Update Engine before Launch whenever the file
   set changes.
5. **Owner feel test** on the prototype, then the rest of the matrix.

## Acceptance

- [x] Player roots on a moving-entry concentration cast — owner-confirmed live 2026-08-29
      ("player rooting works").
- [x] NPC roots on a real mage that demonstrably wants to move — owner-confirmed live
      2026-08-29 on a spawned `LvlWarlockNecromancer` in forced pursuit (initially reported
      unrooted, retracted on a second spawn: "this npc is rooted afterall"). Telemetry agrees:
      `evidence/t58/01-npc-warlock-displacement-poll.csv` — interior cast-span speed 2.6 u/s
      mean (max 24) vs 102 u/s mean (max 483) between casts, three separate frozen spans on
      both hands. Note the telemetry alone would have been the ticket-33 false positive; the
      owner's eyes are the closing evidence, the CSV is corroboration. The generalizable
      lesson (ticket 33 running in reverse — there telemetry said green and the feel said
      broken; here the first glance said broken and the second spawn plus the CSV said green):
      **one actor's behavior is one sample, whichever direction it points** — a single owner
      glance included. Resolve a disagreement by producing another sample, not by arguing from
      the numbers; the CSV earned its place by surviving the disagreement.
- [ ] Rotation tracking still works during the cast — owner question pending.
- [x] Every end path exits the state cleanly — a full combat's worth of repeated casts with no
      wedge; player held-channel enter/3.2s-hold/release clean in SpellHotbar2.log
      (`channel held 3209ms`, `SH2_CastExit -> true`).
- [x] The contention table exists (`notes/58-contention-table.md`); the patch touches only the
      four clear state nodes plus the three declaration nodes, so no displacements at all.
- [x] No Papyrus, no SPID, no ESP records in the shipped mechanism — the patch is 14 Nemesis
      text files.
- [ ] FOMOD option, after the mechanism is owner-certified — not before.

## Build and verification record (2026-08-29)

**The mechanism as shipped** (mod code `shcr`, "Spell Hotbar 2 - Casting Commitment",
generated by `.scratch/shcr-build/build_shcr.py`, validated by `validate_shcr.py`):

- The key simplification found reading Magelock's graph: all four of its per-state replacement
  machines route their NPC branch to ONE identical node — a `hkbModifierGenerator` wrapping a
  `bAllowRotation` `BSIsActiveModifier` (`#altmag$51/$52`) around `#altmag$86`, a copy of
  vanilla `LeftHandMagicCast_MSG` (`#0088`) whose only delta is a staff-flavor rework
  (`#altmag$87` at selector indices 0 and 8). Dropping the player/NPC split and the staff
  rework leaves: `#shcr$0` (binding set) + `#shcr$1` (the rotation modifier) + `#shcr$2..$5`
  (four thin modifier-generators over vanilla `#0088`), swapped into the `generator` param of
  `#0926`/`#0930`/`#0965`/`#0998`. One new variable (`bAllowRotation`) declared in
  `#0077/#0078/#0079`. 14 files total, zero contested nodes.
- `shcc` retired: patch tree, `.scratch/shcc-build/`, and its FOMOD option removed (commit
  `3af9755`). `shcc` was already unticked in Nemesis; the deployed copy was deleted from
  `Dev - Spell Hotbar 2`. The retire list is now closed (2026-08-29, later the same day): the stale `shcc`
  comments are gone because the code they annotated is gone. The inert DLL movement
  capture is deleted — `equipped_hand_concentration_cast_active`,
  `concentration_root_is_swallowing_movement`, `is_movement_control` and the gate in
  `input/input.cpp`, plus the three predicates in `casts/combo_cache.h` and their tests.
  Its gate required `bAnimationDriven`, which ticket 54 measured never rising on this
  cast, so it never fired once. ADR-0015 gets its fourth amendment: the shcr mechanism
  recorded as a generator replacement rather than a flag plant, and the 2026-08-29
  layered-state amendment withdrawn along with the code it authorized. `combo_cache_test`
  green; DLL links clean (the post-build copy into `Dev - Spell Hotbar 2` failed because
  a live Skyrim held the file — nothing was deployed).
- Nemesis: `-Tick shcr -Apply -UpdateEngine` — Engine update 121 s, generation 115 s,
  1058 animations, clean. All five `SHCR_*` objects byte-verified in the compiled
  `magicbehavior.hkx`; zero `SHCC` residue.
- The FF cell was never at risk per the owner: MSCO's full-body cast states already own
  fire-and-forget. The initial NPC non-rooting report was concentration, and retracted.

**Testing notes for whoever comes next:** console-command capture was dead this session
(commands queued, capture markers never landed) — the papyrus tool did everything instead,
including `ObjectReference#PlaceAtMe`/`MoveTo`/`StartCombat` for the NPC fixture, which works
fine through `self={"form":...}`. Papyrus calls 504 while the owner sits in a menu (VM frozen)
and calls on a deleted ref never resolve — neither is a fault. Both test warlocks were
spawned in-memory and deleted; nothing was saved during the session.
