# Separate core behavior from load-order adaptations

The Core Fork will contain changes only when Spell Hotbar 2's generally applicable native behavior must adapt. Records, presets, configuration, and other Nolvus-specific fixes will live in a separate Compatibility Package, keeping the maintained source intelligible without baking one load order into the product.
